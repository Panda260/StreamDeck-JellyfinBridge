name: Build and Publish Docker image

on:
  push:
    branches:
      - "**"
  pull_request:
    branches:
      - "**"
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    concurrency:
      group: docker-${{ github.ref }}
      cancel-in-progress: true
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Set up Node (for potential pre-steps)
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata (tags, labels)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: |
            ghcr.io/${{ github.repository }}
          tags: |
            # Tag = Branchname (z.B. main, feature-xyz)
            type=ref,event=branch
            # Zusätzlich ein SHA-Tag für eindeutige Builds
            type=sha
            # latest NUR für main
            type=raw,value=latest,enable=${{ github.ref == 'refs/heads/main' }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: |
            # Add build args if you later externalize any tokens

  # Optional: scan (kann später aktiviert werden)
  # scan:
  #   needs: build-and-push
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Pull image
  #       run: docker pull ghcr.io/${{ github.repository }}:sha-${{ github.sha }}
  #     - name: Trivy scan
  #       uses: aquasecurity/trivy-action@0.20.0
  #       with:
  #         image-ref: ghcr.io/${{ github.repository }}:sha-${{ github.sha }}
